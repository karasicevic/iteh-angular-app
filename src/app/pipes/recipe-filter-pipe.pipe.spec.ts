import { RecipeFilterPipe } from './recipe-filter-pipe.pipe';

describe('RecipeFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
