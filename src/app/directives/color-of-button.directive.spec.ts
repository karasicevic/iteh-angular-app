import { ColorOfButtonDirective } from './color-of-button.directive';

describe('ColorOfButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorOfButtonDirective();
    expect(directive).toBeTruthy();
  });
});
